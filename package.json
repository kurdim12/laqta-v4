{
  "name": "laqta-v3",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "LAQTA v3 — the live-event photo platform of MaraNasi",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "typecheck": "tsc -b --noEmit false --emitDeclarationOnly false --pretty",
    "build:test": "VITE_API_URL=http://localhost:8787/api vite build",
    "gate:phase1": "npm run build:test && playwright test tests/gate/phase-1",
    "gate:phase1:full": "npm run build:test && LAQTA_OFFLINE_MINUTES=10 playwright test tests/gate/phase-1",
    "gate:phase2": "npm run build:test && playwright test tests/gate/phase-2",
    "gate:phase2:full": "npm run build:test && LAQTA_CUT_MINUTES=5 playwright test tests/gate/phase-2",
    "gate:phase3": "npm run build:test && playwright test tests/gate/phase-3",
    "gate:phase4": "npm run build:test && playwright test tests/gate/phase-4",
    "gate:phase5": "npm run build:test && playwright test tests/gate/phase-5",
    "gate:phase6": "npm run build:test && playwright test tests/gate/phase-6",
    "gate:phase7": "npm run build:test && playwright test tests/gate/phase-7",
    "gate:phase7:full": "LAQTA_OFFLINE_MINUTES=10 npm run gate:phase7",
    "gate:all": "npm run gate:phase1 && npm run gate:phase2 && npm run gate:phase3 && npm run gate:phase4 && npm run gate:phase5 && npm run gate:phase6 && npm run gate:phase7"
  },
  "dependencies": {
    "@imgly/background-removal": "^1.4.5",
    "@imgly/background-removal-data": "^1.4.5",
    "qrcode": "^1.5.4",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.26.2"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "@types/qrcode": "^1.5.6",
    "@types/react": "^18.3.11",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.2",
    "typescript": "^5.6.3",
    "vite": "^5.4.9"
  }
}
